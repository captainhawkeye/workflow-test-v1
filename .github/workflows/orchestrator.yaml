name: icp-orchestrator

on:
  push:
    branches:
      - pre-main
  workflow_dispatch: {}

jobs:
  run-ci:
    uses: ./.github/workflows/ci.yaml

  run-cd:
    needs: run-ci
    uses: ./.github/workflows/cd.yaml
